# Tasks: Tailwind & Theme Setup (Hito 4.1.1)

1. [ ] Instalar la tipografía Geist (`npm install geist`).
2. [ ] Editar `tailwind.config.ts` para añadir la paleta de colores "vento" y la fuente "Geist Sans".
3. [ ] Limpiar `src/app/globals.css` y configurar los CSS variables para los colores base y Dark Mode.
4. [ ] Crear un componente de prueba `src/components/ThemePreview.tsx` que use los colores y fuentes definidos.
5. [ ] Añadir una página `src/app/design-tokens/page.tsx` para visualizar toda la paleta y tokens durante el desarrollo.
6. [ ] Validar que el build no muestra errores de configuración.

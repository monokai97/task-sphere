# Tasks: Shadcn Core Integration (Hito 4.1.2)

1. [ ] Ejecutar `npx shadcn@latest init` y configurar las rutas siguiendo `src/components/ui`.
2. [ ] Instalar la utilidad `clsx` y `tailwind-merge` para el helper `cn()` requerido por Shadcn.
3. [ ] Añadir `src/lib/utils.ts` con el helper `cn()`.
4. [ ] Instalar componentes base: `button`, `input`, `checkbox`, `dialog`, `card`.
5. [ ] Verificar que cada componente se ha descargado correctamente en `src/components/ui/`.
6. [ ] Probar un componente (ej. `Button`) en una página para confirmar que no hay conflictos de clases de Tailwind.
7. [ ] Confirmar que el `components.json` está correctamente referenciado en el root del proyecto.

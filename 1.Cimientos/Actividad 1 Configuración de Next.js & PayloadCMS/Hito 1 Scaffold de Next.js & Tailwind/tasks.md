# Tasks: Scaffold de Next.js & Tailwind (Hito 1.1.1)

1. [ ] Ejecutar `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm`.
2. [ ] Limpiar el contenido por defecto de `src/app/page.tsx` para dejar una estructura mínima "Vento Style".
3. [ ] Editar `tailwind.config.ts` para incluir los tokens de diseño (colores neutros y radios de borde suaves).
4. [ ] Configurar `src/app/globals.css` eliminando los estilos por defecto de Vercel y aplicando variables CSS para Dark Mode.
5. [ ] Instalar la tipografía Geist (`npm install geist`) e integrarla en el layout raíz.
6. [ ] Validar que `npm run build` genere el bundle sin advertencias.
